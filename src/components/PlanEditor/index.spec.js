describe('PlanEditor', () => {
  /**
   * As a user, I see a spinner when the app is loading the plan
   * The component handles HTTP failures
   * The component handles HTTP successes
   * The component handles HTTP successes with GraphQL errors
   * As a user, I see the GraphQL errors returned by the API
   * 
   * TODO: ACs related to what is displayed on the screen
   */
});